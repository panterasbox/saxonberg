/**
 * MaturationProfile — the instanceable concrete over the `lib/maturation/`
 * substrate class (the Material precedent: the abstract home stays in
 * `lib/`, templates name THIS path). Every ferment-profile row's
 * `class:` is `/platform/idea/maturation/MaturationProfile`; rows themselves
 * live under their owning pack's `idea/maturation/` subtree.
 */

import MaturationProfileBase from '../../../lib/maturation/MaturationProfile';

export default class MaturationProfile extends MaturationProfileBase {}
