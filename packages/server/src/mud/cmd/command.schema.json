{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://saxonberg.dev/schemas/command.schema.json",
  "title": "Saxonberg Command Definition",
  "description": "YAML command spec consumed by CommandApi. Each .yaml file in this directory must validate against this schema.",
  "type": "object",
  "required": [
    "verbs",
    "description"
  ],
  "additionalProperties": false,
  "properties": {
    "verbs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Command verbs and aliases. The first entry is the primary verb."
    },
    "controller": {
      "type": "string",
      "minLength": 1,
      "description": "Class name of the CommandController to dispatch to. Optional only when every declared subcommand carries its own `controller:` (Option E \u2014 per-subcommand controller templates)."
    },
    "description": {
      "type": "string",
      "description": "One-line human-readable command description. Shown in verb lists and as the help header."
    },
    "help": {
      "type": "string",
      "description": "Multi-line authored help prose for the verb \u2014 what it does, when to reach for it, gotchas. Rendered below the synthesized syntax block by `getHelpText()`. Plain text (MML-safe); blank lines separate paragraphs."
    },
    "examples": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/example"
      },
      "description": "Worked invocations shown under an Examples heading. Sparing by convention \u2014 only where a concrete invocation teaches something the generated usage cannot. Each is an individually searchable {cmd, note} pair."
    },
    "args": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/positional"
      },
      "description": "Positional args for the (flat) verb. Mutually exclusive with `subcommands`."
    },
    "subcommands": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/subcommand"
      },
      "description": "Subcommand-keyed dispatch map. Mutually exclusive with `args`."
    },
    "options": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/option"
      },
      "description": "Verb-scoped options."
    },
    "payload": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/option"
      },
      "description": "Structured-form-only fields. Populated exclusively via CommandApi.assembleFromStructured \u2014 the text-input path doesn't surface these to the matcher. Same field shape as options (type / schema / scope / validators / multiple / default), minus the keyboard-focused fields (short / prepositions / greedy don't apply)."
    },
    "validators": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/validatorSpec"
      },
      "description": "Verb-level validators that fire BEFORE field validators with `context.commandGiver` populated. Used for command-shape preconditions (animacy, mobility, vocal capacity) that don't tie to a specific arg."
    },
    "fallthrough": {
      "type": "boolean",
      "description": "Opt-in flag permitting top-level `args:` AND `subcommands:` to coexist. When set, the matcher tries subcommands first; on an unknown first token it falls through to bind against the top-level `args:` (Phase 3a). Required together with both `args:` and `subcommands:` \u2014 schema rejects any other combination."
    },
    "async": {
      "type": "boolean",
      "description": "Default async-dispatch mode for this verb (default false). When true, the controller body detaches from the giver's own input chain at accept-time (spawned, not awaited), so the giver's prompt frees immediately and the single dispatch-response envelope fires late. A player overrides per-invocation with the reserved `--async` / `--sync` flags. Only for commands whose output tolerates arriving out of order (never `look`); see command-spec.md."
    }
  },
  "if": {
    "properties": {
      "fallthrough": {
        "const": true
      }
    },
    "required": [
      "fallthrough"
    ]
  },
  "then": {
    "required": [
      "args",
      "subcommands"
    ]
  },
  "else": {
    "not": {
      "required": [
        "args",
        "subcommands"
      ]
    }
  },
  "$defs": {
    "example": {
      "type": "object",
      "required": [
        "cmd"
      ],
      "additionalProperties": false,
      "properties": {
        "cmd": {
          "type": "string",
          "minLength": 1,
          "description": "The literal command line a player would type."
        },
        "note": {
          "type": "string",
          "description": "Short gloss explaining what the example does or demonstrates."
        }
      }
    },
    "subcommand": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": "string"
        },
        "help": {
          "type": "string",
          "description": "Multi-line authored help prose for this subcommand, rendered under the subcommand's usage line in `help <verb>`."
        },
        "examples": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/example"
          },
          "description": "Worked invocations specific to this subcommand. Sparing by convention."
        },
        "controller": {
          "type": "string",
          "minLength": 1,
          "description": "Optional per-subcommand controller template name. When present, the framework clones THIS controller for the subcommand instead of the verb-level controller (Option E). Verbs whose subcommands all carry per-subcommand controllers may omit the verb-level `controller:` entirely."
        },
        "args": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/positional"
          }
        },
        "options": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/option"
          }
        },
        "validators": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/validatorSpec"
          },
          "description": "Subcommand-level validators that fire AFTER the verb-level validators and BEFORE field validators with `context.commandGiver` populated, only when this subcommand is the one invoked. The subcommand-scoped equivalent of verb-level `validators` \u2014 authority/precondition gates that apply to one subcommand (e.g. a founder-only `assign`) without blocking the verb's other, public subcommands."
        }
      }
    },
    "positional": {
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "requires": {"oneOf":[{"type":"string","minLength":1},{"type":"array","items":{"type":"string","minLength":1},"minItems":1}],"description":"What this object-typed slot ACCEPTS, by mixin composition. `any` = deliberately unconstrained (a wizard's `destruct <anything>`, an MQL selector, a perception verb that refuses nothing by kind). Otherwise a mixin name from the Mixins registry — `SealableMixin` — which the bound Stuff must compose; the framework synthesises the check and the refusal sentence. A LIST means ALL of them; `|` inside one entry means EITHER (`CombustibleMixin|FurnaceMixin`). An unknown name is a load-time error, which is the point: `targetKind: any`, its predecessor, was an unverifiable promise and three of its fifty uses were wrong. Kind only — relation (`canReach`) and state (already open, currently lit) stay in validators and controllers."},
        "name": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
          "description": "Field name the bound value lands on. Reserved: `subcommand` (when the verb declares subcommands)."
        },
        "type": {
          "enum": [
            "string",
            "number",
            "boolean",
            "object",
            "objects",
            "struct"
          ],
          "description": "Field value type. `object` runs `MqlApi.resolveOne` (single-cardinality MQL); `objects` runs `MqlApi.resolveMany` (plural-cardinality MQL); `struct` is a structured-input-only blob (Record<string, unknown>) \u2014 text input cannot bind it."
        },
        "schema": {
          "type": "object",
          "description": "Optional JSON Schema fragment validated against the structured value before user-defined validators fire. Only meaningful for `type: struct`. Runs through ajv during the structured-input coercion step."
        },
        "required": {
          "type": "boolean"
        },
        "greedy": {
          "type": "boolean",
          "description": "Greedy positional: consumes the remainder of input verbatim. Must be the last arg in its array."
        },
        "multiple": {
          "type": "boolean",
          "description": "Accumulate multiple values into an array. Used for non-MQL fields; for MQL plural use `type: objects` instead."
        },
        "validators": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/validatorSpec"
          }
        },
        "default": {
          "type": "string",
          "description": "Fill-in text used when the player provides no input for this field. Runs through ShellApi.expandVariables \u2014 `default: \"$focus\"` resolves to the giver's current focus at bind time."
        },
        "scope": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 1
            }
          ],
          "description": "MQL fragment(s) the dispatcher tries when resolving this field. Each entry runs through ShellApi.expandVariables and is tried in order; first non-empty result wins. Array form is the explicit fallback chain (e.g. `['$focus', 'inventory, here']` for drill-first). Only meaningful for `type: object` / `type: objects`."
        },
        "updates_focus": {
          "enum": [
            "extend",
            "replace",
            "none"
          ],
          "description": "Focus management policy. `extend` appends the input fragment to the giver's current focus with `:` (with same-anchor compaction); `replace` re-anchors wholesale; `none` (default) leaves focus alone. Inspection-shaped verbs (look, examine, open, close) declare `extend`."
        },
        "prepositions": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Optional list of leading prepositions the matcher consumes (e.g. `[at]` for `look at`, `[to]` for `give X to Y`)."
        },
        "cardinality": {
          "$ref": "#/$defs/cardinality"
        },
        "onExcess": {
          "enum": [
            "top",
            "take-all",
            "prompt",
            "truncate",
            "error"
          ],
          "description": "Policy when MQL resolves more results than cardinality permits. See command-spec.md."
        },
        "onShortage": {
          "enum": [
            "error"
          ],
          "description": "Policy when MQL resolves fewer results than cardinality.min. v1 only value: 'error'."
        }
      }
    },
    "cardinality": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min": {
          "type": "integer",
          "minimum": 0
        },
        "max": {
          "type": "integer",
          "minimum": 0
        },
        "exactly": {
          "type": "integer",
          "minimum": 0
        }
      },
      "description": "Authorial cardinality constraint on an `objects` field. Defaults to { min: 0, max: Infinity }. `exactly: N` is sugar for { min: N, max: N }."
    },
    "option": {
      "type": "object",
      "required": [
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "requires": {"oneOf":[{"type":"string","minLength":1},{"type":"array","items":{"type":"string","minLength":1},"minItems":1}],"description":"What this object-typed slot ACCEPTS, by mixin composition. `any` = deliberately unconstrained (a wizard's `destruct <anything>`, an MQL selector, a perception verb that refuses nothing by kind). Otherwise a mixin name from the Mixins registry — `SealableMixin` — which the bound Stuff must compose; the framework synthesises the check and the refusal sentence. A LIST means ALL of them; `|` inside one entry means EITHER (`CombustibleMixin|FurnaceMixin`). An unknown name is a load-time error, which is the point: `targetKind: any`, its predecessor, was an unverifiable promise and three of its fifty uses were wrong. Kind only — relation (`canReach`) and state (already open, currently lit) stay in validators and controllers."},
        "short": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1,
          "description": "Single-character short alias (e.g. `-v` for `--verbose`)."
        },
        "type": {
          "enum": [
            "boolean",
            "string",
            "number",
            "object",
            "objects",
            "struct"
          ]
        },
        "schema": {
          "type": "object",
          "description": "Optional JSON Schema fragment validated against the structured value before user-defined validators fire. Only meaningful for `type: struct`."
        },
        "scope": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 1
            }
          ],
          "description": "MQL fragment(s) the dispatcher tries when resolving this option's value. Same precedence + expander rules as a positional's `scope`. Only meaningful for `type: object` / `type: objects`."
        },
        "field": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
          "description": "Field name the bound value lands on. Defaults to the option's own name."
        },
        "required": {
          "type": "boolean",
          "description": "Used by payload fields (the structured-form-only family) to declare that the client MUST attach this key. Verb-scoped options ignore this \u2014 options are by convention optional."
        },
        "multiple": {
          "type": "boolean"
        },
        "default": {},
        "description": {
          "type": "string"
        },
        "validators": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/validatorSpec"
          }
        },
        "cardinality": {
          "$ref": "#/$defs/cardinality"
        },
        "onExcess": {
          "enum": [
            "top",
            "take-all",
            "prompt",
            "truncate",
            "error"
          ]
        },
        "onShortage": {
          "enum": [
            "error"
          ]
        },
        "effects": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/phaseEffect"
          },
          "description": "Lifecycle effects this option applies to the dispatcher when its bound value is truthy. Each entry names a `phase` from the documented vocabulary (focus-update / validate / confirm-prompt / dispatch / emit-scene) and an `action` (`skip` or `replace`). `replace` carries a `with` handler name. Schema-valid effects against phases or handlers the substrate hasn't wired yet throw at runtime."
        }
      }
    },
    "phaseEffect": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "phase",
        "action"
      ],
      "properties": {
        "phase": {
          "enum": [
            "focus-update",
            "validate",
            "confirm-prompt",
            "dispatch",
            "emit-scene"
          ],
          "description": "Dispatcher lifecycle phase the effect targets. See the phase taxonomy in api/command.ts for implementation status \u2014 schema-valid phases without runtime support throw at dispatch time."
        },
        "action": {
          "enum": [
            "skip",
            "replace"
          ],
          "description": "`skip` bypasses the phase. `replace` runs a framework-registered handler in place of the phase's default behavior; requires `with`."
        },
        "with": {
          "enum": [
            "deferred-dispatch",
            "explain-plan"
          ],
          "description": "Replacement-handler name. Required when action is `replace`. The vocabulary is documented up-front; throws at dispatch time if the handler hasn't been implemented yet."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "action": {
                "const": "replace"
              }
            }
          },
          "then": {
            "required": [
              "with"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "action": {
                "const": "skip"
              }
            }
          },
          "then": {
            "not": {
              "required": [
                "with"
              ]
            }
          }
        }
      ]
    },
    "validatorSpec": {
      "type": "string",
      "pattern": "^(/|\\./|\\.\\./).+",
      "description": "Validator path. Must start with `/` (mud-rooted absolute) or `./` / `../` (relative to YAML)."
    }
  }
}
