verbs: [tell, whisper]
controller: TellController
description: "Send a private message to someone"
syntax:
  - pattern: "<target> <message...>"
    description: "Tell someone a private message"
    fields:
      target:
        type: text
        required: true
      message:
        type: text
        required: true
