verbs: [cancel]
controller: system/CancelController
description: "Cancel an in-flight activity. With no argument, cancels every activity. With a type ('reading', 'walking', ...), cancels matching activities."
args:
  - name: type
    type: string
    required: false
