verbs: [close]
controller: boundary/CloseController
description: "Close a door (by direction or by name)"
validators:
  - /lib/command/validators/requiresAnimate
args:
  - name: target
    type: object
    required: true
    scope: "reachable"
    updates_focus: extend
    validators:
      - /lib/command/validators/canReach
