verbs: [spill]
controller: bulk/SpillController
description: "Spill a vessel's contents onto the floor"
validators:
  - /lib/command/validators/requiresAnimate
args:
  - name: target
    type: object
    required: true
    scope: "reachable"
    validators:
      - /lib/command/validators/mustBeVisible
