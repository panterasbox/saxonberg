/**
 * MQL permissions — operator/seed access control by tier.
 *
 * Phase 6 will implement the tier check; Phase 5 will define the
 * operator/seed → tier map for new admin-tier seeds.
 */

export {};
