/**
 * MQL scope-walk — given a scope MQL fragment and a query, walks each
 * resolved anchor's neighborhood (own keywords, contents, details,
 * environment one level up, exits) and scores candidates.
 *
 * Phase 5 will implement this module.
 */

export {};
