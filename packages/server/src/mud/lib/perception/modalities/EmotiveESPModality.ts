/**
 * EmotiveESPModality — emotive-broadcast-ESP singleton. Field
 * modality per the hybrid framing (local aether field + network
 * routing on top).
 *
 * v1 `signalAt` returns the inherited null default — future emote
 * verbs ride this modality as the carrier; the field-walk is
 * reserved for the ESP-physics build.
 */

import { Modality } from '../Modality';

export class EmotiveESPModality extends Modality {}
