# The yard behind the house on lot 2 — the KEYED room, and the only place
# in the game where a player can grow something on ground they own.
#
# This template is cloned PER LOT by the LotHolder and keyed on the lot's
# parcel extent, exactly as `dormroom.yaml` is cloned per unit and keyed
# on its unit parcel. Title and durable state therefore share one
# identity: the row that says you own the ground is keyed the same as the
# record that remembers what is growing on it.
#
# THE HOUSE IS PROSE. It is `details:` on this room rather than an
# interior, deliberately: the residence build is furnishing real interiors
# in parallel, and a house that is described rather than built can be
# upgraded later instead of rebuilt. What phase 2 is about is the ground
# behind it.
#
# `populates:` lays the bed down once, on first provisioning, through the
# born-with path — after which the yard's own record carries it and the
# specs sit unused (never double-seeded).
# `/obj/TitledRoom`, not a plain CartesianLocation: this room's contents
# are the owner's, so it has to be a PERSISTENCE HOST — a plain
# CartesianLocation persists nothing it holds, and `restoreOrSeed` throws
# on it. It is also non-singleton, because one template is cloned per lot.
class: /obj/TitledRoom
hydratorClass: /lib/persistence/PersistentHydrator
data:
  shortDescription: the yard behind the house
  primaryKeyword: yard
  alternateNames: [garden, back, plot]
  _address: terminus/hinkley-hills/yard
  _biomePath: /lib/biome/outdoor/baseline
  # Full open sky — the whole point of being out here. A carrot wants 120
  # lux to be unlimited by light; a windowsill gives 30.
  ambientIntensity: 600
  ambientColorTemperature: 5800
  longDescription: |
    Grass, a wire fence, and the back of the house — a white weatherboard wall with two windows and a step down from the kitchen door. The ground falls away west just enough to catch the afternoon.

    Somebody dug a bed here and framed it in boards, and then either ran out of time or ran out of reasons. It is good soil under the grass; the rest of the lot is still lot.
  coords:
    x: -1
    y: 1
    z: 0
  exits:
    south:
      destination: /domain/terminus/hinkley-hills/lane
  populates:
    - /obj/bed/garden
    - /domain/terminus/hinkley-hills/standpipe
  details:
    house:
      keywords: [house, wall, weatherboard, windows, door, kitchen, step]
      description: |
        Low, white, and older than the survey — the farmhouse that was here before anyone drew lots over the paddock. Two windows onto the yard, a kitchen door with a worn step, and a gutter that wants clearing. Inside is dim and quiet and yours; from out here it is mostly a wall to keep the wind off the bed.
    fence:
      keywords: [fence, wire, boundary]
      description: |
        Four strands of wire on split posts, straight enough. It marks the lot rather than defends it — everything past it is grass nobody owns yet.
    grass:
      keywords: [grass, lot, ground, slope]
      description: |
        Rough pasture grass gone to seed, knee-high where it has not been walked. Underneath it the soil is dark and deep; this was a paddock before it was a subdivision, and it has not forgotten.
