# TravelCredentialUpdate — the fast-travel credential as a hosted update
# (the incorporeal twin of the TravelCard Thing). Injected into an
# attunement host by Avatar.installDefaultLoadout. Born with the
# University Avenue floor (set on TravelCredentialMixin); session-durable
# (re-cloned each login), so the registered set resets to the floor.
class: /lib/fasttravel/TravelCredentialUpdate
hydratorClass: /lib/persistence/PersistentHydrator
data: {}
