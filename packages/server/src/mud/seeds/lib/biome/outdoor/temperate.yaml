# /lib/biome/outdoor/temperate — FolderZone organizing the 15 temperate
# leaves (quad / forest / meadow / etc.). The shared temperate
# defaults live on the `temperate/baseline` Biome leaf.
class: /lib/zone/FolderZone
hydratorClass: /lib/persistence/PersistentHydrator
data:
  name: temperate
