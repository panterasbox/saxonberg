# Demo chair — exercises the alt-unit literal mass authoring shape
# (`mass: "12000 g"` ⇒ converted via the g↔kg unit converter to
# `Quantity<kg>(12)`).
#
# Material binding: oak (a wood with chemistry data of its own —
# the `composition` field lets `analyze chemistry of chair` read
# leaf-element fractions).
#
# Lives under /obj/demo/ as throwaway showcase content.
class: /obj/demo/DemoItem
hydratorClass: /lib/persistence/PersistentHydrator
data:
  name: wooden chair
  long: A heavy oak chair with a high back and broad armrests. Solid enough to seat anyone who fits.
  _materialPath: /lib/material/wood/oak
  mass: "12000 g"
