# trade-brewing

The brewing trade, as a stub: materials (`ale`, `lager`), the `keg`/`cask` presets, two floor kegs at target in the brewery outfit's stock, the outfit's hand consigning them onto `/trade/distilling/thing/counter`, and the `pint` recipe. Production (the ferment) is the distillery build's — see `docs/slates/builds/libations-slate.md` Part 1.
