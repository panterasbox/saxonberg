# corpo-goodkin

The `domain` kind: the Corpo mark (`/obj/corpo/Corpo/goodkin`) and every Brand whose `data.owner` is `goodkin`, adopted in place from the former `mud/seeds/obj/corpo/` tree. The `/corpo/goodkin` Organization entry in `mud/bootstrap.ts` and the `config/groups.yaml` board row stay platform-seeded (D12).
