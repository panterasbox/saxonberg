# corpo-aevex

The `domain` kind: the Corpo mark (`/obj/corpo/Corpo/aevex`) and every Brand whose `data.owner` is `aevex`, adopted in place from the former `mud/seeds/obj/corpo/` tree. The `/corpo/aevex` Organization entry in `mud/bootstrap.ts` and the `config/groups.yaml` board row stay platform-seeded (D12).
