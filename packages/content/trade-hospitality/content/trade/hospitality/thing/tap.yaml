# The keg tap — the dispensing station (hospitality's `Tap`: a Surfaced
# fixture that is a `tap` tool). The keg it draws from is a bulk holder in
# the same room; the `pint` recipe requires the tap and draws the beer.
class: /trade/hospitality/thing/Tap
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  shortDescription: a beer tap
  longDescription: A brass tap handle over a drip tray, the line running down behind the bar to the keg.
  primaryKeyword: tap
  keywords: [tap, taps, beer-tap, handle]
  mass: 12
