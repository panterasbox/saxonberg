# trade-produce

The produce trade, as a stub: eight produce materials (tag = category), their Provision items, the `crate`/`basket` presets, eight crates at target in the produce stock, and the produce outfit consigning onto `/trade/distilling/thing/counter`. No recipe — produce is an input (`press` is hospitality's).
