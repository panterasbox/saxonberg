id: saxonberg-lounge
version: 0.1.0          # reserved; nothing reads/enforces it (non-goal: version machinery)
# The document root: the retired ScriptSeeder's owner path, so every
# existing dev DB ADOPTS its three rows in place (same path, same owner).
root: /domain/lounge
description: The lounge — this wave only its three world scripts (msh); rooms/NPCs are wave 4.
# The ids exist by wave 2 step 6; unknown deps are ignored in v1 (orderByDependsOn).
dependsOn: [platform, corpo-goodkin, corpo-vionne]
