# The Dyehouse Yard — the dyeing trade's ground.
#
# ⚠ At the INDUSTRY ROOT rather than at `/trade/dyeing/location`: a
# class row must be at `<root>/<branch>/<leaf>` (`lint:instanceable`'s
# invariant 7), and the industry root is where a zone covering the whole
# trade belongs anyway.
class: /platform/idea/location/CartesianZone
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  name: The Dyehouse Yard
  cellSize: 4.0
