{
  "name": "@saxonberg/content-generic-objects",
  "version": "0.1.0",
  "private": true,
  "description": "Generic objects — the object clusters and loose rows every locality names, plus the small classes those rows need (the pack ships a src/: SconceLamp, the wall-mounted light the residences build hangs).",
  "type": "module",
  "dependencies": {
    "@saxonberg/content-platform": "workspace:*",
    "@saxonberg/server": "workspace:*",
    "@saxonberg/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "yaml": "^2.8.2"
  }
}
