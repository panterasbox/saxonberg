# corpo-veshko

The `domain` kind: the Corpo mark (`/obj/corpo/Corpo/veshko`) and every Brand whose `data.owner` is `veshko`. The `/corpo/veshko` Organization entry in `mud/bootstrap.ts` and the `config/groups.yaml` board row stay platform-seeded (D12).
