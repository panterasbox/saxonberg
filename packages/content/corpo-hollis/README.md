# corpo-hollis

The `domain` kind: the Corpo mark (`/obj/corpo/Corpo/hollis`) and every Brand whose `data.owner` is `hollis`, adopted in place from the former `mud/seeds/obj/corpo/` tree. The `/corpo/hollis` Organization entry in `mud/bootstrap.ts` and the `config/groups.yaml` board row stay platform-seeded (D12).
