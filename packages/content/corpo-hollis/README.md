# corpo-hollis

The `domain` kind: the Corpo mark (`/obj/corpo/Corpo/hollis`) and every Brand whose `data.owner` is `hollis`. The `/corpo/hollis` Organization entry in `mud/bootstrap.ts` and the `config/groups.yaml` board row stay platform-seeded (D12).
