# VisionModality — the vision singleton.
# Field modality; signalAt walk lands in Phase 2 (relocated from
# the retired LightApi.lightAt).
class: /obj/modalities/VisionModality
hydratorClass: /obj/persistence/PersistentHydrator
data:
  name: vision
  family: field
  modality: vision
