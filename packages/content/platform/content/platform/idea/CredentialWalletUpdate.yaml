# CredentialWalletUpdate — the born-with credential wallet app, the single
# hosted holder replacing the PaymentImplantUpdate + TravelCredentialUpdate
# twins. Injected into an attunement host by Avatar.installDefaultLoadout.
# Ships an empty payment record (openAccount auto-links accounts on relog) and
# a travel record (re-floored to the born-with University Avenue node).
# Session-durable (re-cloned each login).
class: /platform/idea/CredentialWalletUpdate
# The born-with payment + travel credential records come from
# CredentialWalletUpdate.defaultCredentialKinds.
data: {}
