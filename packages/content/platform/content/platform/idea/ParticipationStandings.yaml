# ParticipationStandings — the singleton whose postRegister warms the
# participation aggregate read-cache and arms the command-dispatch tap +
# the real-time recompute schedule (the self-warming shape; eager via
# this pack's boot list, not an AppBootstrap sequencer line).
class: /platform/idea/ParticipationStandings
data: {}
