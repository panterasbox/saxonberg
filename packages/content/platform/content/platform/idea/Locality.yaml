# /platform/idea/Locality — admin/ownership root for the addressing content area.
#
# This is a FolderZone (NOT a Locality) — the template tree's role here
# is to scope write access for the addressing team and hold the
# Locality leaves beneath it. The addressing NAMESPACE (the rooted
# `narnia/...` tree the Localities claim prefixes in) is independent of
# this templatePath tree; the divergence is deliberate.
class: /platform/idea/FolderZone
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  name: address
