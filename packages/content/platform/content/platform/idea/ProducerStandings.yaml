# ProducerStandings — the singleton whose postRegister warms the
# producer aggregate read-cache and arms the attributed-engagement tap +
# the real-time recompute schedule (the self-warming shape; eager via
# this pack's boot list, not an AppBootstrap sequencer line).
class: /platform/idea/ProducerStandings
data: {}
