verbs: [lie]
controller: /platform/idea/cmd/posture/LieController
description: "Lie down on something. With no argument, lie on the ground."
help: |
  Stretch out flat. Bare `lie` lies down where you are, on the
  ground. Name a target — a bed, a couch, a patch of grass — to
  lie on that instead, if it can hold someone lying down.

  Lying is a posture and stays put until you change it. Get back
  up with `stand`, or shift to `sit` or `kneel`.
validators:
  - /lib/command/validators/requiresAnimate
  - /lib/command/validators/requiresPosed
  - /lib/command/validators/requiresSlottable
  - /lib/command/validators/requiresEmbodied
args:
  - name: target
    type: object
    required: true
    default: "ground"
    scope: ["$focus", "reachable"]
    requires: [VisibleMixin, PosturedMixin]
    # `sit on the ground` is the form a person actually types. Without
    # `prepositions`, CommandLogic binds `on` AS the target and the
    # leftover word trips "too many arguments" into shape-fall-through;
    # `greedy` then lets the multi-word tail arrive whole, and the MQL
    # desugar drops the article, so `the ground` reaches the keyword
    # match as `ground`.
    prepositions: [on, at, in, upon]
    greedy: true
