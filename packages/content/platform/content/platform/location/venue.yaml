# The bare venue — the room `ArchetypeApi.materialize` clones and fills
# with an archetype's default bindings (the derived test venue, A13.5).
# Nothing populates it; the archetype does.
class: /platform/location/FurnishableRoom
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  name: a venue
  description: A bare venue, waiting to be fitted out.
  keywords: [venue, room]
