# treasury — the Schedule of Parameters' TREASURY rows (economic bootstrap
# D4): what the fisc advances and on what terms. Operator-set via `config`
# this build (a `treasury set` is a seam, not a wave); the Minister of
# Finance APPROPRIATES — they do not tune.
settings:
  # The Arrival Note's principal (minor units): what the Treasury advances
  # a newcomer against the note the machine writes at `embody confirm`.
  # Rate 0 — the Compact's rate for newcomers; discharged by the first
  # wage or by the game-days below; secured by the balance it funded and
  # nothing else. NO LABOR IS EVER OWED.
  - key: treasury.arrivalPrincipal
    value: "20"

  # The Note discharges on its own after this many game-days active,
  # wage or no wage.
  - key: treasury.noteDischargeGameDays
    value: "30"

  # The standing facility: a business's OPENING ADVANCE (minor units) — a
  # 0% loan from the treasury on its first account, secured by that
  # account, repaid as a share of inflows like any loan. A loan precisely
  # so the state never owns the residual of every business in the world.
  #
  # ⭐ A FLOAT, not capital (economic bootstrap W7): enough to complete a
  # house's first supplier terms — the purchase history rung 1 reads —
  # and deliberately LESS than one restocking beat, so inventory is
  # financed where the design puts it: at a chartered bank, at the
  # window, against the goods. At two thousand no shop ever borrowed and
  # the ladder was theater; at fifty the general store's keeper completes
  # three purchases on the float and presents to Goodkin for the fourth.
  - key: treasury.openingAdvance
    value: "50"

  # ── the estate clocks (economic bootstrap D16) ─────────────────────────
  # Real DAYS of absence, read off the member's last logout. The short
  # clock: dormant — the account freezes (credits still land), a seat
  # held is vacant, a shop kept is closed, a house sleeps; a login lifts
  # it. The long clock: escheated — on the next touch the estate passes,
  # debts first, then situs up the title tree; the balance sits in the
  # treasury as UNCLAIMED property, reclaimable forever. ⚠ Tune these low
  # for a drive; ship them long — a member is not a lapsed subscription.
  - key: estate.dormantAfterDays
    value: "30"
  - key: estate.escheatAfterDays
    value: "180"
  # After how many real days of absence a seat held by a member is VACATED
  # (the record flips to `vacated`, a terminal exit — never resurrected by
  # the roster; the house account is unlinked). Shorter than dormancy: a
  # chart cannot wait a month for a teller.
  - key: employment.absenceVacatesAfterDays
    value: "14"
