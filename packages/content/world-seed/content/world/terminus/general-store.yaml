# The General Store — a one-room building zone off the counting-houses avenue
# block (the third /world/terminus/ area, after the terminal and the
# counting-houses). A CartesianZone holding the shop floor. "The general store"
# rides the zone `name` + the room prose (a generic room, no NamedMixin).
#
# Owned by the Terminus municipality — NOT declared here (the governing
# security invariant); ownership lives in the gated `parcels` collection
# (`/world/terminus/general-store` → the managed `terminus` group; see
# mud/config/parcels.yaml). The store owns its *Business* (its P&L account),
# not the street it stands on.
class: /platform/idea/location/CartesianZone
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  name: The General Store
  cellSize: 3.0
