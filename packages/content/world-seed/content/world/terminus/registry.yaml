# Terminus Registry — the city administration's one built department (the
# civics flagship): a rented annex off the station frontage where the
# young municipal corporation keeps its records and seats its acting
# Magistrate. A small CartesianZone (the terminal.yaml precedent) holding
# the public counter room. Ownership is NOT declared here — it lives in
# the gated `parcels` collection (`/world/terminus/registry` → the
# managed `terminus` group; mud/config/parcels.yaml), the governing
# security invariant.
class: /obj/location/CartesianZone
hydratorClass: /obj/persistence/PersistentHydrator
data:
  name: Terminus Registry
  cellSize: 3.0
