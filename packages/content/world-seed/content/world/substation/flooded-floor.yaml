# The flooded switch-cell floor — the brine pool that grounds you and carries
# the current. A `Floor` (Adornment) with an authored **surface bulk** pool of
# salt water (~40 L standing water), attached to the cell via its `adornments:`
# map. The conductive medium the shock model reads: `ElectricityLogic` finds
# this pooled floor, sees a conductive surface material + a non-zero amount,
# and grounds anyone standing in it. Mirrors `/stuff/thing/surface/default-floor` (the
# posture `ground:1` slot + the `floor` detail) with the pool authored on top.
class: /platform/thing/Floor
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  shortDescription: flooded concrete floor
  # `ground` added by `lint:ground` clause (d) — see the gate.
  keywords: [floor, ground, flooded, concrete]
  register: indefinite
  longDescription: A concrete floor under a skin of brackish standing water.
  # ⭐ Concrete, cast and laid — `slab` indoors. The row has said "concrete"
  # in its keywords and its prose since it shipped and carried no material;
  # `ceramic/concrete` is the row that makes the claim true. Authored DRY
  # (5e-4 S/m): what grounds you in this cell is the brine POOL below, as it
  # always was, not the slab under it.
  _materialPath: /stuff/idea/material/ceramic/concrete
  worked: true
  # Surface-bulk: an authored brine pool (the conductive medium + ground).
  surfaceBulk: true
  surfaceMaterial: /stuff/idea/material/bulk/salt-water
  surfaceAmount: 40
  staticSlots:
    - name: 'ground:1'
      accepts: SlottableMixin
      capacity: 9007199254740991
      postures: [sit, lie, kneel, stand]
      userFacingDetail: floor
  # ⚠⚠ **No `floor` / `ground` DETAIL.** There used to be one here, and the
  # drive is what found it: `look floor` bound the DETAIL rather than the
  # floor object, and a detail's description renders WITHOUT the host's
  # `markupAugmenters` — so the derived sentence naming the material was
  # invisible in exactly the place an author would look for it. The detail
  # was a workaround for a floor not being addressable by its own name;
  # `FloorMixin` unions `floor` and `ground` onto the class, so the object
  # answers and the self-referential detail is now redundant AND harmful.
  # `lint:ground` clause (f) refuses one. A detail naming a real sub-feature
  # — the crossing's worn track, the yard's gutter — is the opposite case
  # and is exactly what details are for.
