# A clean bandage — the canonical first-aid dressing (DressingMixin(Thing)).
# `treat`/`bind`/`dress` consumes one to dress a bleed. Stocked in the
# glass alley (where you get cut) so the treat loop is playable in-world.
class: /obj/Bandage
hydratorClass: /obj/persistence/PersistentHydrator
data:
  shortDescription: a clean bandage
  primaryKeyword: bandage
  longDescription: A roll of clean white gauze, the kind kept in every first-aid tin. Enough to dress a wound once.
  dressingQuality: 1
