# The Practicum — the magic demonstrator zone: a small practice hall where
# the whole v1 spell roster is castable at authored targets, plus the
# warded cell proving suppression. A self-contained CartesianZone, its own
# home (the hearthworks precedent). No inbound exit wired this cycle —
# reachable by teleport / a future travel node (the treeline reachability
# precedent keeps content-area standup clean).
class: /obj/location/CartesianZone
hydratorClass: /obj/persistence/PersistentHydrator
data:
  name: The Practicum
  cellSize: 3.0
