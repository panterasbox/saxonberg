# trade-winemaking

The winemaking trade, as a stub: materials (`red`, `white`, `sparkling`, `dry-vermouth`, `sweet-vermouth`), the `wine-bottle` preset, five floor bottles, the vintner outfit consigning onto `/trade/distilling/thing/counter`, and the `glass-of-*` recipes. Vermouth is winemaking's (fortified wine) and depends on distilling for its spirit — the dependency is declared, the recipe is the distillery build's.
