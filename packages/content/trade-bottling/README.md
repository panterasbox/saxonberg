# trade-bottling

The bottling trade, as a stub: the mixer and juice materials, `ice` (frozen water — `meltingPoint`, `latentHeatOfFusion`), the `can`/`mixer-bottle` presets, eight floor rows incl. the `ice-bag`, the bottling outfit consigning onto `/trade/distilling/thing/counter`, and the `mixer` recipe.
