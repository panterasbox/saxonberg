# The floor outfit's floor — a `warehouse` instance: the racking, the
# outfit's Stock (the floor rows' home `container:` — the sweep stands them
# at target here) and the hand whose `consigns` beat carries them to the
# cash-and-carry. No exits: the hand teleports (the `shifts` shape); a walk
# is the locomotion slate's.
class: /platform/location/FurnishableRoom
hydratorClass: /platform/idea/persistence/PersistentHydrator
data:
  shortDescription: the floor outfit's floor
  primaryKeyword: floor
  keywords: [floor, yard, warehouse, floor]
  longDescription: |
    A working yard behind a roller door: racking, a pallet of cased bottles by the dray, and the smell of spirit in the sawdust.
  ambientIntensity: 25
  ambientColorTemperature: 4200
  populates:
    - /trade/distilling/thing/racking
    - /trade/distilling/thing/floor-stock
    - /trade/distilling/agent/floor-hand
