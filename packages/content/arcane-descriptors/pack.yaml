id: arcane-descriptors
version: 0.1.0          # reserved; nothing reads/enforces it
description: >-
  Descriptor banks — the pools an unidentified magic item draws its
  appearance from. One bank per item class, each the product of two
  DECORATIVE axes (never material).
dependsOn: []
