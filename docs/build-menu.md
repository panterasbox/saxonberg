# Build menu — pick a build by how much you've got tonight

This is not a backlog. Backlogs get ordered by *priority* (an opinion you
stop believing) or by *area* (which doesn't match how you decide), and
then they get abandoned. This list is ordered by **size**, because the
real question at the start of a session is *how much time and brain do I
have right now* — and that axis doesn't go stale.

"Big" is two things bundled: **build volume** (how much code) and
**design-openness** (how much undecided thinking). An end-of-night pick
wants both *low* — mechanical, already decided. A day story wants
design-openness *high* — that's the fun. The tiers are tagged
accordingly.

Every item rides shipped substrate unless noted. Full detail on any pick
is one click away in the by-area reference, [slates/README.md](./slates/README.md);
this file is just the menu. Cross items off as they ship; add new ones at
the tier that fits. Sizes are rough — move anything that feels mis-tiered.

---

## 🌙 Nightcap — an evening, design settled, mostly mechanical

- **GFM table sugar** — accept `| a | b |` table input in the MML renderer. Pure parser. *(message-rendering)*
- **`whois` / `locate` verb** — surface the already-captured country (+ city/region) as a verb; data's already on `Interactive`. *(connection-origin)*
- **Player nicknames** — `name X as Y` writing into the existing belief store; recognition reads it. *(recognition)*
- **Admin time verbs** — `setScale` / `pause` / `resume` on the world clock; already sketched, just wire. *(time)*
- **Hangover & deficiencies** — content + conditions on the shipped metabolism toxin system (scurvy, the morning after). *(metabolism)*
- **Affordance source-scoping** — the `watch::set` sigil so a verb afforded by many objects disambiguates by source. *(affordance-verb)*

## ☕ Half-day — one contained wave, light design

- **Console search & filters** — Ctrl-F, sender-filter, family-mute in the tabbed terminal (client). *(console-filtering)*
- **Fast-travel as living infrastructure** — terminals break down / get maintained; the inert `status` seam goes live. *(fast-travel)*
- **Bulkable mixing & gas** — solutions/mixing + a gas reservoir on the bulk substrate (the alchemy on-ramp). *(bulkable)*
- **Encumbrance cart hinge** — hand carried weight off to a wheeled conveyance (the designed "hinge"). *(encumbrance)*
- **Host-slot activities** — Mount/Sit/Lie/Drive as real engaged activities (sketched with code already). *(host-slot-activities)*
- **Smoke / contaminant breathing** — light the already-laid `contaminant` column → toxin-via-lungs (couples to metabolism). *(respiration)*

## 🌞 Day story — a full day+, real design decisions to make

- **Weather Wave 2** — fog→visibility, rain→wetness (feeds the thermal wet-collapse), snow depth, hazards. *(weather)*
- **Thermal Wave 2** — phase-change/ice (latent-heat reserve) + frostbite + per-region coverage + sauna. *(thermal)*
- **Locomotion as activity** — `TraverseActivity` + sneak/crawl modes + trap/sound seams. *(locomotion-as-activity)*
- **`analyze X with Y`** — the pedagogical identification instrument: real Material chemistry, partial ID, misidentification. *(identification)*
- **Map client pane** — a 2D/3D spatial renderer over the honest coordinate system. *(map)*
- **Scripting piping** — multi-stage pipelines over the built `Pipeline` AST node + the value→field binder. *(scripting)*

## 🏔️ Expedition — multi-day epic, big open design (the meaty ones)

- **Vitals application wave** — live condition progression + the death-transition driver + assessment/treatment/forensics. *(vitals)*
- **Language substrate** — `Language` Idea + `read`/`Readable` + proficiency + species native tongues + partial comprehension. *(language)*
- **Advancement game-layer** — guilds-as-institutions + player-extensible skill content-graph + the loadout economy (atop the shipped measurement core). *(advancement)*
- **Cooperative governance** — the three chambers / parliamentary executive / sortition judiciary (atop the stake-ledger). *(cooperative)*
- **Help + wiki** — the systems rulebook (HelpTopic index) + community wiki, with the spoiler-dial model. *(help + wiki)*
- **Eternal University + deduction** — the campus content area + the forensics quest spine. *(eternal-university)*
