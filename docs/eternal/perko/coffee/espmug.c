inherit ObjectCode;
 
void extra_create()
{
  set( "id", ({
    "empty espresso mug",
    "espresso mug",
    "empty mug",
    "mug",
    }) );
  set( "short", "an empty Posh Perko Pit espresso mug" );
  set( "long",
    "This small, empty espresso mug is made from white "
    "ceramic and has \"The Posh Perko Pit\" printed in "
    "green on the outside of it." ); 
  set( "value", 10 );
  set( "weight", 5 );
  set( "read", "\"The Posh Perko Pit\" is written on the "
    "outside of the mug in green letters." );
}
