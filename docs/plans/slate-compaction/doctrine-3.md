# Doctrine-homing pass — batch 3

Branch `design/doctrine-homing`. Procedure:
`.claude/skills/compact-slate/SKILL.md` § The doctrine-homing pass. One
row per Doctrine entry of the assigned ledgers — `unlinked-4` ·
`fasttravel` · `maturation` · `combat` · `locomotion`. Home decided by the
STATUS of what the doctrine argues for, verified against
`packages/server/src/**` and `packages/content/**`, never the slate's
own markers. Every MOVE's text is verbatim under `## Handoff` at the end;
this agent inserts into no top-level doc.

Insertable subsystem docs for this batch: `fasttravel.md` ·
`maturation.md` · `combat.md` · `locomotion.md`. The `unlinked-4`
ledger's slates have no insertable doc — anything that would graduate
from them into a subsystem doc goes to Handoff naming that doc.

## Rows

### unlinked-4

- `unlinked-4` · `tails/hand-slot-slate.md` · (no Doctrine entries) — nothing to home.
- `unlinked-4` · `builds/wizard-duty-slate.md` · `# ⭐ The premise, stated without comfort` + `## ⭐⭐ And the case that matters most here is not snooping` — **STAYS** — argues for the unbuilt duty regime (no `su`, no break-glass, no declared-purpose log anywhere under `packages/server/src` / `packages/content`); its first half (*TS access subverts the whole apparatus*) is already `access.md § The code-trust lockdown`'s opening sentence, so nothing graduates; the operator-forgeability half is the premise requirements will need.
- `unlinked-4` · `builds/wizard-duty-slate.md` · `# ⭐⭐⭐⭐ The law the technology suggests: constrain by DUTY` + `## ⭐⭐ A wizard is a wizard regardless of position` + `### ⚠ And the recursion has an answer, up to a point` — **STAYS** — the thesis the whole `Left` list serves (duty text, safe harbour, revocation process: none built); the AGPL-as-exit line is the manifesto's already and needs no second home.
- `unlinked-4` · `builds/wizard-duty-slate.md` · `# ⭐⭐⭐ Carve-outs: prohibition is the wrong shape` — **STAYS** — the shape of an unbuilt declared-purpose mechanism (nothing declares a purpose at use).
- `unlinked-4` · `builds/wizard-duty-slate.md` · `## ⭐⭐⭐ The meta-design that makes it hold` + `## ⚠ The honest limit, and why it is acceptable` — **STAYS** — the convenience-of-the-sanctioned-path argument is the design rule for the unbuilt break-glass instrument; the memory's *resilience posture* is a working agreement, not a doc, so no DUPLICATE target exists.
- `unlinked-4` · `builds/wizard-duty-slate.md` · `# The mechanisms, ranked by whether they survive a hostile wizard` — **STAYS** — doctrine with an embedded backlog (JIT elevation, two-person rule, public roster — all unbuilt, all open as Q4/Q5).
- `unlinked-4` · `builds/authoring-intelligence-slate.md` · `### The two-level composition vocabulary` → the *"The catalog family is open-ended — and decoupled from the runtime"* paragraph — **GRADUATED → handoff (`studio.md § The blueprint catalogue`)** — the catalogs SHIPPED (`BlueprintCatalogue.ts`, `StudioLogic.ts`'s `listMixins`/`describeMixin`; brains re-resolve per invocation per `behavior.md`) and `studio.md` carries the what (the cache note at l.20, § The blueprint catalogue) without the why (*a catalog is never load-bearing for runtime correctness; the worst failure is a palette omission*); no insertable doc in this ledger, so the verbatim text is in Handoff below and the slate paragraph is left in place until the coordinator's insert lands (cut-after-diff rule).
- `unlinked-4` · `builds/room-condition-design-pack.md` · `## Part 2 — ⭐⭐ The Law-2 line, and the doctrine correction` (*we model the mess you MAKE, not the mess TIME makes*) — **STAYS** — argues for an UNBUILT mechanism (no `Soilable.ts` under `packages/server/src`; `Dyed.test.ts:165` asserts its absence); the realm-level classification it rests on (archetype 1 — act-driven, freezes in absence) is already `docs/stewardship-doctrine.md § Part 1`'s table row, which points back here, so nothing MOVEs; the line is the rationale the build's tests will assert.
- `unlinked-4` · `builds/land-use-covenant-slate.md` · `## Three limits on one act — do not collapse them` — **STAYS** — the covenant row is UNBUILT (`covenant` appears nowhere under `packages/server/src` / `packages/content` except `Epoch.ts` / `Tooled.ts` comments; `AccountabilityKind` is still four); the anti-collapse rule is the requirement its build will be judged by.
- `unlinked-4` · `builds/land-use-covenant-slate.md` · `## ⭐⭐ Hard or soft — the lenses decide it…` (*a refusal message with no diegetic cause*) — **STAYS** — the law-vs-physics split it argues for is unbuilt on the law side; `design-lenses.md` is not a MOVE target in the pass's table and the line is a lens-3 application, not a new realm truth — revisit for `antipatterns.md` when the build lands.
- `unlinked-4` · `builds/land-use-covenant-slate.md` · `## Enforcement — a producer, not a chokepoint` (*hidden covenants are not a thing*) — **STAYS** — the breach producer is unbuilt; the honesty rule is `concealment.md`'s honest-fog family applied to a thing that does not exist yet.
- `unlinked-4` · `builds/saxonberg-city-slate.md` · `## The doctrine — the frame is ours; the furniture is theirs` — **STAYS** — no City of Saxonberg locality exists (the `/world` roots are unchanged; no `Government` row for the city); the never-half-grown reconciliation is the acceptance rule of an unbuilt build, and its home is the locality's own doc once one exists.
- `unlinked-4` · `builds/saxonberg-city-slate.md` · `## The short-term build` item 4 → the **venue doctrine** (*venues are never workplaces … no mechanic may ever require presence for governance*; ceremony · spectacle · legibility · address) — **STAYS** — the seven venues are unbuilt and the rule binds their shape; ⚠ flagged for the coordinator: the negative half (*no vote cast in a room, no quorum of bodies*) is a governance-wide rule the shipped `office` verb already obeys and `governance.md` / `civics.md` never state — a candidate `governance.md § Why nothing requires presence` if the coordinator prefers to home it now rather than with the city build; the text is quoted in Handoff for that option only.
- `unlinked-4` · `builds/saxonberg-city-slate.md` · `## What Saxonberg is` → *the allowance is an honestly-labeled out-of-fiction meter … it never wears a diegetic costume* — **STAYS** — the second Compact grant is unbuilt (the stewardship cascade has not shipped); the metaresource guardrail lives in no top-level doc today (only `stewardship-slate`, `balance-slate` and siblings), so there is no DUPLICATE target and no listed MOVE target owns the topic.
- `unlinked-4` · `builds/grid-slate.md` · `# Part 1` (*coverage is legal, connection is physical* · *the meter is the boundary*) — **STAYS** — the network is UNBUILT (`ParcelRecord` has no service field, exits carry no service attribute, no lint); the meter-as-boundary is the design rule of the walk that does not exist.
- `unlinked-4` · `builds/grid-slate.md` · `# Part 2` (*Path is location. Title is ownership.* · *a class with no unique state is decoration*) — **STAYS** — street dedication is unbuilt; the first line is `parcel.md`'s premise quoted in support (one clause of an unbuilt section, not cuttable below the paragraph) and the `Street.ts` refusal is a build-time recommendation the build must honour — `antipatterns.md` has no such entry yet and gains one when the refusal is exercised, not before.
- `unlinked-4` · `builds/grid-slate.md` · `## ⭐⭐⭐ And this is what magic is FOR — the frontier tier` (*a community's development arc IS the replacement of magic by infrastructure*) — **STAYS** — the arc has two tiers and only the frontier tier shipped (τ charge economy, `ManaPowered` terminals, the arming floor); the grid tier is unbuilt, so the doc cannot yet carry an arc between them; the shipped half's why (*a battery decouples power from place, which lets an author put a powered thing on the frontier without lying*) is already `magic-items.md § Why a home has no line, and why the cell is a story` — nothing further graduates until a wire exists.
- `unlinked-4` · `builds/grid-slate.md` · `## ⭐⭐⭐ Build the METER, never the RULE` (*the engine models service and access as FACTS; it never models requirements*) — **STAYS** — argues for the unbuilt service declaration + lint; the general form (*a statute is a constraint on a meter*) is `balance-slate`'s and no top-level doc in the MOVE table owns it (`measurement.md` is about what may be counted, not about law over meters).
- `unlinked-4` · `builds/grid-slate.md` · `# Open questions` Q4's blockquote (*a utility can overcharge, but it cannot pay or bill you in something only it issues*) — **STAYS** — its premises (currency records are code; a ledger leg never crosses currencies) are shipped and already `banking.md § Currency` / `§ Conservation`'s; its conclusion is about utility billing, which is unbuilt (Q4 itself is open), so it waits beside the billing design.
- `unlinked-4` · `builds/intervention-slate.md` · `## The gap` (*any design that arrives here answers "what does the engine honestly measure?" first*) — **STAYS** — the section frames an UNBUILT build (`CombatantState.side` still a bare string, no pair relation in `CombatGraph`); the parley lesson inside it is already `combat.md § Why there is no verb for talking a fight down` (DUPLICATE in substance) but shares its paragraph with the build's own measurement answer (*presence, force, position and risk are all things the engine already counts*), and paragraphs are not split.
- `unlinked-4` · `builds/intervention-slate.md` · `## ⭐⭐ Standing — who may do this` (*does standing change what you MAY do, or only what it COSTS you afterward?*) — **STAYS** — an open design question of an unbuilt build (the slate itself calls it the user's call); the gate-vs-price rule goes to `accountability.md` when a producer actually applies it.
- `unlinked-4` · `builds/intervention-slate.md` · `## ⭐⭐ The two poles that exist, and the hole between them` (*a coup is a discrete scheduled act, a fight is a standing relation*) — **STAYS** — the reason the build is a build; `intervene` and `defend` shipped and `combat.md` describes both, but the argument is about the hole between them, which is unbuilt.

### fasttravel

- `fasttravel` · `tails/fast-travel-slate.md` · `## Why teleport (and why it's magic)` → paragraph 2 (*the network's reach is the un-genre thesis made navigable; the route map is a content surface for it*) — **STAYS** — the thesis half is already `fasttravel.md § Why teleport, and not a train` verbatim in substance (*no business on the same map … the network's reach is the thesis made navigable*), i.e. DUPLICATE, but it shares the paragraph with the unbuilt route map (no route-map content or EU staging Stop exists under `packages/content/tpa/`), and paragraphs are not split; nothing to insert.
- `fasttravel` · `builds/mana-economy-slate.md` → now `builds/mana-economy-design-pack.md § Absorbed from mana-economy-slate.md — What this does to the wood question (Doctrine)` (moved by the cluster pass, `cluster-mana.md`) · `# Part 7 — What this does to the wood question` — **STAYS (contradicted)** — its second bullet's premise (*mana does not lift that ceiling, because mana is energy*) is false under `arcane-science.md § The second quantity — mana is not energy`, and the ⚠ note beside it already says so; the surviving thesis (*magic relocates an industry, never replaces one*) argues for a mana grid + refrigeration industry that are unbuilt, and `arcane-science.md § The Postulate` already carries the physics half (*the postulate relocates; it does not manufacture*), so nothing MOVEs.
- `fasttravel` · `builds/mana-economy-design-pack.md` · `## Part 10 — Pedagogy` — **STAYS** — the teaching case for an UNBUILT pack (no deposit, refinery, farm, assay trade or tiered sale exists under `packages/content/arcana/src/` — the pack ships devices and `ManaPowered` only); `arcane-science.md`'s course table already carries the one row that is realm-level today (*conserved quantities and coupling constants*), the rest are lessons of mechanisms that do not exist and belong beside their requirements.
- `fasttravel` · `builds/mana-economy-design-pack.md` · `## Part 5` → `### The consequence that shapes everything: the mana industry is MUNDANE` — **STAYS** — argues for the unbuilt extraction industry (*mages are customers, not producers*); its physics premise is already `arcane-science.md § The power level` (*you cannot build a mana plant; you can only own a mana deposit*), so nothing MOVEs; the industry thesis waits for the build.

### maturation

- `maturation` · `tails/supply-chain-slate.md` · `# Part 4 — ⭐⭐⭐ The stepping stone: spot market → contracts → the firm` (the four-stage table + the Coase paragraph + the stage-3 paragraph) — **MOVED → handoff (`docs/settlement-model.md § 7. The ladder`, as `### Absorbed from supply-chain-slate — the stepping stone: spot market → contracts → the firm`)** — every rung's mechanism SHIPPED (`ConsignController.ts` / `BuyController.ts`; `contract.md`'s escrowed clauses; employment + parcel title) and the slate's own 2026-09-20 note says rungs 2–4 are content gaps, not design; the transaction-cost driver is a realm-level truth about why firms form here, and *Coase* / *transaction cost* appear in no doc (`grep -ri` over `docs/*.md docs/subsystems/*.md`: nothing) — `settlement-model.md § 7` carries the business ladder (tool → stall → premises → roster → position) without the market-organization ladder beside it. Slate text left in place pending the coordinator's insert; after it lands the slate keeps the heading, a one-line pointer and the *Verified 2026-09-20* paragraph (which `Left`'s rung 2–4 items cite). ⚠ Coordinator's call: `maturation.md` is this batch's insertable doc but is the wrong home (it is the durative transform, not market organization) — if the coordinator judges the Coase ladder outside `settlement-model.md`'s remit, the row reverts to STAYS.
- `maturation` · `tails/supply-chain-slate.md` · `## Freight is the OPTIMIZATION, not the prerequisite` (the von Thünen paragraph) — **MOVED → handoff (`docs/settlement-model.md § 8. Connective tissue`)** — both halves SHIPPED (the supply chain per the slate's own status; `Lane` / `Route` / the Journey per `logistics.md`), *von Thünen* appears in no doc, and the skill names it `settlement-model.md`'s topic; ⚠ **overlaps `doctrine-5.md`'s handoff of the fuller triple from `towns-slate § The connective tissue` to the same § 8** — the coordinator applies ONE absorbed block and this slate's two sentences ride under it (its one distinct clause: *the supply chain is what makes it happen; freight is the optimization, not the prerequisite*). Slate paragraph left in place pending that insert.

### combat

- `combat` · `builds/consequence-slate.md` · `## ⛔ The rejected fork — recorded so nobody re-proposes it` — **DUPLICATE → `combat.md § Poise` (*Poise stays combat-scoped — rejected, not merely undone*, l.87–103)** — already graduated by the compaction pass itself and already cut from the slate (the slate carries only a three-line pointer at l.87–91); verified the text is in `combat.md`; nothing further to do.

### locomotion

- `locomotion` · `tails/collision-slate.md` · `### "Sleeping body in the doorway"` → the three bullets (*no sub-room positioning · walk past trivially · description can flavor*) — **DUPLICATE → `docs/design-philosophy.md § How this lands for collisions and blocking`** — the doc already states the bag-of-stuff default for exactly this case (*a sleeping body in your path … sub-room positioning is explicitly deferred*); the three bullets + their lead line are CUT from the slate (−7/+3, one pointer sentence); the section's second paragraph (the `BlockerBehavior` opt-in with an `unblockCondition`) is unbuilt and stays.
- `locomotion` · `tails/locomotion-as-activity-slate.md` · (no Doctrine entries — the ledger records *none beyond the responsiveness argument, which graduated with the duration section*) — nothing to home.

## Totals

- GRADUATED: 1 (→ handoff, `studio.md`; no insertable doc in that ledger)
- DUPLICATE: 2 (`combat.md § Poise` — already cut; `design-philosophy.md § How this lands for collisions and blocking` — cut this pass)
- MOVED → handoff: 2 (both `settlement-model.md`)
- STAYS: 23
- STAYS (contradicted): 1
- entries with nothing to home: 2 (`hand-slot-slate`, `locomotion-as-activity-slate` — ledgers list none)

Files touched by this agent: `docs/plans/slate-compaction/doctrine-3.md`
(new) · `docs/slates/tails/collision-slate.md` (−7/+3). No subsystem doc
was inserted into: the one graduation and both moves are Handoff items,
and the only DUPLICATE with text still in a slate was a cut.

## Handoff

Verbatim text for the coordinator to apply. Nothing below has been
cut from its slate yet; cut after the insert lands (diff it).

### 1 · GRADUATE → `docs/subsystems/studio.md` § The blueprint catalogue

Insert as a short `#### Why the catalogs may be loose` subsection at the
end of § The blueprint catalogue (or directly under the `blueprints`
cache note at l.20 — either reads). Source:
`docs/slates/builds/authoring-intelligence-slate.md § The two-level
composition vocabulary`, the paragraph beginning *"The catalog family is
open-ended"*. ⚠ One link inside it points at
`../builds/npc-behavior-slate.md`; in the doc it should point at
[behavior.md](./behavior.md) (brains re-resolve per invocation is that
doc's shipped fact). Otherwise verbatim:

> **The catalog family is open-ended — and decoupled from the runtime.** Mixins
> and combos are the first two members; **brains** (NPC behavior modules — see
> [npc-behavior-slate.md](../builds/npc-behavior-slate.md)) are a third, and hooks /
> validators / other path-resolved module kinds will follow. They all share one
> governing property: **the catalog is a CMS-side artifact the game framework
> never depends on.** The runtime only ever does **path-resolution** — it
> follows an explicit reference, lazy-loads, re-resolves; it never enumerates
> and never reads a catalog. So a catalog is allowed to be as presumptuous as
> the CMS likes — eagerly indexed, or even **partly hand-maintained** ("someone
> keeps it up to date") — because it is **never load-bearing for runtime
> correctness**. The worst failure of a stale or wrong catalog is a **palette
> omission** (a real module that exists at a path doesn't show in the picker
> until the catalog refreshes); you can still reference it by path *today* and
> it works. A catalog bug can degrade the authoring UI; it can never break a
> running game. That safe failure mode is *why* catalogs get to be loose — and
> why discovery (tree-walk / lazy per-scope index / curation) is exclusively
> this layer's concern, not the framework's.

After the insert: replace the slate paragraph with one line — *the
catalog-decoupling principle → studio.md § Why the catalogs may be
loose*.

### 2 · OPTIONAL — `docs/subsystems/governance.md` (the venue doctrine)

Recorded as STAYS above; quoted here only so the coordinator can home
the governance-wide negative rule now if preferred. Source:
`docs/slates/builds/saxonberg-city-slate.md § The short-term build`,
item 4, the paragraph beginning *"The venue doctrine"*. If applied, a
`## Why nothing requires presence` section; the slate paragraph then
becomes a pointer (the seven venues themselves stay — unbuilt).

> **The venue doctrine (user, 2026-08-01 — the wizards-council
> lesson): venues are never workplaces.** Every MUD that built a
> council chamber learned the same thing: governing happens in the
> channels and on the forums, because communication pools where
> friction is lowest, and nobody prefers the `say` command to
> working from home. So no mechanic may ever require presence for
> governance — no vote cast in a room, no quorum of bodies, no
> attendance-gated anything. What a venue honestly provides is
> exactly four things, all compatible with standing empty:
> **ceremony** (thresholds bear it — a swearing-in, a ratification
> day; a calendar of occasions, not office hours), **spectacle**
> (the live floor as optional theater feeding the async machine —
> and a stage for the broadcast track), **legibility** (the
> reading-room function works at zero attendance: the machine's
> state, in the world, behind a door a tourist can open), and
> **address** (a place to point at, petition to, film in front
> of). “Will trials happen in the courthouse? Probably never —
> and the point was never attendance.”

### 3 · MOVE → `docs/settlement-model.md` § 7. The ladder

Insert after the rung-4 paragraph (*"Rung 4 is never purchasable…"*), as
`### Absorbed from supply-chain-slate — the stepping stone: spot market →
contracts → the firm`. Source: `docs/slates/tails/supply-chain-slate.md
§ Part 4`, the table + the two paragraphs that follow it (NOT the
*Verified 2026-09-20* paragraph, which is the slate's own status note
and stays there). ⚠ The `contract.md` link is written slate-relative
(`../../subsystems/contract.md`); from `settlement-model.md` it is
`./subsystems/contract.md`. Otherwise verbatim:

> > **User: "is that a stepping stone to a different model?"**
> 
> Yes — and each stage is **already a shipped subsystem**, so the progression
> needs nothing invented. What changes is only which is *cheapest*:
> 
> | Stage | Mechanism | Cost it removes | Cost it adds |
> |---|---|---|---|
> | **1 — Spot market** | consignment + `buy` at the store | ⭐ **no capital to sell; no counterparty risk** | the store's commission; the walk |
> | **2 — Direct purchase** | `buy` at the farm | the commission | **counterparty risk** returns; you must find each other |
> | **3 — Forward contract** | [contract.md](../../subsystems/contract.md) clauses + escrow | ⭐ **harvest uncertainty** | capital locked in escrow |
> | **4 — Vertical integration** | employment + parcel title | the market entirely | you now run a farm |
> 
> > ⭐⭐⭐ **The driver of the whole progression is TRANSACTION COST — which is
> > literally Coase.** The firm exists because using the market has costs.
> > Here the commission and the haul **are** those costs, so vertical
> > integration emerges exactly when organising internally is cheaper than
> > transacting. **Not a metaphor — the actual mechanism, and it will be
> > observable.**
> 
> ⭐ Stage 3 is the one agriculture historically *invented*, and for the reason
> the game supplies free: **harvests are lumpy** (crops mature on a clock)
> while a distillery wants steady input. A forward contract is the answer, and
> `contract.md` already does clauses over verifiable conditions.

After the insert: the slate keeps the `# Part 4` heading, a one-line
pointer (*→ settlement-model.md § 7, Absorbed from supply-chain-slate*)
and the *Verified 2026-09-20* paragraph that `Left`'s rung 2–4 items
cite.

### 4 · MOVE → `docs/settlement-model.md` § 8. Connective tissue

⚠ **Overlaps `doctrine-5.md`'s handoff** (`towns-slate § The connective
tissue` → the same § 8, proposed heading `### Absorbed from towns-slate —
the realm layout is already von Thünen`). Apply ONE absorbed block; this
slate's paragraph adds one clause the towns triple lacks — *freight is
the optimization, not the prerequisite; the supply chain is what makes
von Thünen happen* — and can ride under that heading as a closing
paragraph. Source: `docs/slates/tails/supply-chain-slate.md § Freight is
the OPTIMIZATION, not the prerequisite`, verbatim (the `freight-slate`
link is slate-relative; from `settlement-model.md` it is
`./slates/builds/freight-slate.md`):

> [freight-slate](../builds/freight-slate.md) earns its place when volume exceeds a
> backpack: a cart carries more than a person, and **vehicles are durative**,
> so time is the cost.
> 
> > ⭐⭐ **Von Thünen falls straight out**: the further from market, the more of
> > a crop's value is eaten by haulage — so land use sorts by distance,
> > unauthored. The freight slate already expects this; **the supply chain is
> > what makes it happen.**
> 

After the insert: the slate's subsection heading stays with a one-line
pointer to § 8.

## For the coordinator to decide

1. **Handoff 3 (the Coase ladder → `settlement-model.md § 7`).** The
   pass's MOVE table names `settlement-model.md`'s topics as *towns /
   von Thünen / the two halves / natural monopoly*; the
   market-organization ladder is a sibling of its § 7 business ladder
   and appears in no doc. If that is judged outside the doc's remit,
   the row reverts to STAYS with no other change.
2. **Handoff 4 vs `doctrine-5.md`.** Two agents are handing the same
   realm truth to the same section; apply one block.
3. **Handoff 2 (the venue doctrine)** is optional — recorded STAYS.
4. **Handoff 1 (`studio.md`)** is a graduation the `unlinked-4` ledger
   could not insert for lack of an insertable doc; nothing in the pass's
   rules forbids the coordinator inserting into a subsystem doc.

---

## Coordinator (2026-09-22)

- Handoff 1 applied: `studio.md § The blueprint catalogue` gains
  `#### Why the catalogs may be loose` (link re-pointed to behavior.md);
  the authoring-intelligence paragraph → a pointer.
- Handoffs 3 + 4 (`settlement-model.md` § 7 Coase ladder, § 8 von Thünen)
  DEFERRED to the end-of-pass top-level commit, applied once with
  doctrine-5's § 8 handoff so the section gets one block. Handoff 2 (the
  venue doctrine → governance.md) declined: the mechanism it constrains
  is unbuilt; it stays in saxonberg-city-slate.
