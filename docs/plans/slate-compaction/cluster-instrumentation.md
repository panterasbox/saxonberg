# Cluster-merge ledger — instrumentation

**Pass:** slate cluster-merge, 2026-09-21 (`design/slate-cluster-merge`).
**Rule:** duplicated OPEN design only; nothing is lost — every heading
of every secondary file appears in the conservation table with where it
went.

## Files

| file | lines before | role |
|---|---|---|
| `docs/slates/builds/instrumentation-slate.md` | 495 | **canonical** |
| `docs/slates/builds/sampling-and-labs-slate.md` | 316 | secondary — names the canonical as *Parent* |
| `docs/slates/builds/implements-slate.md` | 149 | assigned, **not a cluster member** (see below) |
| `docs/slates/builds/deduction-slate.md` | 224 | assigned, **not a cluster member** (see below) |

## Canonical: `instrumentation-slate.md` — why

- Sampling-and-labs names it explicitly: *"**Parent:**
  instrumentation-slate (the three gates, the aether line,
  siting-by-anatomy — read first)"* and its *What this slate does NOT
  cover* hands the capability/competence/condition gating model and the
  `analyze` retrofit to it.
- It carries the larger remaining design (the retrofit, the
  `MeasuringMixin` model, route-gated `analyze`, the implant rung).
- Memory doctrine names it the check-first reference for any
  measure/analyze/instrument/augment design.
- No build worktree is running from it (`git worktree list`:
  economic-bootstrap · fishing · detached · design/treatment).

Code re-verified 2026-09-21 (unchanged from the 09-19 stamp): no
`MeasuringMixin` / `readings` anywhere in `packages/server/src/mud` or
`packages/content/*/src`; `measure.yaml` still names pack controllers
for `strike` (l.162, trade-mining), `dip` (l.343), `texture`/`acidity`
(trade-farming), `passage` (trade-haulage), `figure` (trade-tailoring);
**eleven** instrument classes in `platform/thing/instrument/`
(Altimeter, Balance, Barometer, GasAnalyzer, GravityMeter,
**Hydrometer**, Hygrometer, Photometer, Sextant, Sundial, Thermometer).

## Why implements and deduction are not cluster members

- **implements-slate** is *items that make a CASTER better* — a magic
  implement folding into `MagicLogic.potencyFactor`. It shares a word
  with instrumentation and nothing else: no section of either file
  cites the other, and the one apparent touch-point (implements'
  *craftability — the lens-maker vocation* vs instrumentation Q6
  *instrument crafting*) is two different objects (a burning-glass vs
  a measuring instrument). Thematic homonym → **touch nothing**; every
  heading tabled as KEPT.
- **deduction-slate** designs a forensic-investigation quest type whose
  first verb is `analyze <thing>` → a finding in the casebook. It
  CONSUMES the canonical's route-gated, competence-banded `analyze`; it
  does not re-design the gate, the readout ladder or any instrument.
  Its *Ties to existing substrates* pointed only at advancement.md for
  that seam, so **one pointer line** was added there naming the
  canonical (the coordinator's "pointers only" allowance). Nothing
  moved; every heading tabled as KEPT.
- **deduction ↔ search-slate** (flagged by the coordinator): read
  `docs/slates/tails/search-slate.md` whole — its subject is the viewer
  half of detection (the equipment term on `effectivePerception`,
  camouflage, spawn-hidden, search posture). It has no evidence / clue
  / forensic / casebook content and deduction designs no search tool.
  **No overlap found; no pointer added** (a pointer to nothing shared
  would be noise).

## Conservation table — `sampling-and-labs-slate.md` (316 → 304 · Status PARTIAL → PARTIAL)

Every heading of the file at `HEAD`, in order. KEPT = the secondary's own
subject (where the toolkit lives, what it costs, what you carry to it);
the canonical carries none of it and should not.

| heading (HEAD line) | outcome |
|---|---|
| `# Sampling & labs slate — the field/bench split…` + status block + framing/Provenance/Parent paragraphs (l.1–40) | KEPT — spine |
| `## Part 0 — The verdict: the four questions, and they are unanimous` (l.42) | KEPT — the field/bench fork is this slate's own decision |
| `### 1. Pedagogy — richest real science` (l.49) | KEPT |
| `### 2. Author expressiveness` (l.71) | KEPT |
| `### 3. Immersion & roleplay` (l.86) | KEPT |
| `### 4. Gamification / self-improvement` (l.98) | KEPT |
| `## Part 1 — ⭐⭐ The structural finding: samplability splits the roster` (l.114) | KEPT — own subject. ⚠ drift noted, not edited: it counts *ten* shipped instrument classes; there are eleven (`Hydrometer` landed since). Its 8 environment-readers / 2 sample-eaters sort is unaffected in kind (the hydrometer reads a captured liquid → the samplable side). The canonical's status block already records the eleven; requirements re-take the table |
| `## Part 2 — Three tiers, not two` (l.144) | KEPT — a precision/cost ladder (naked eye · hand tool · bench); a different axis from the canonical's eyeball · carried · mounted table (how it is carried), so not a duplicate |
| `## Part 3 — The sample object (cheaper than it looks)` (l.162) | KEPT — own subject (the sample + its provenance field, salting, the assayer) |
| `### Sample integrity — the free pedagogical bonus` (l.183) | KEPT |
| `## Part 4 — ⭐ The mining slate is amended, NOT overturned` (l.192) | KEPT — own subject; amends `mining-slate` (outside this cluster, see *For the coordinator*) |
| `## Part 5 — The economy: a capital ladder, and the lab as a place` (l.217) | KEPT |
| `## Part 6 — ⚠ The tedium risk, and the four levers` (l.237) | KEPT |
| `## Part 7 — What this answers in the parent slate` (l.256–272, 18 lines incl. divider) | **MOVED → `instrumentation-slate.md` § *Absorbed from sampling-and-labs-slate — Part 7 — What this answers in the parent slate*** (inserted verbatim after the canonical's *Open questions (for requirements)*, before the 2026-09-01 addendum; the heading is retained in the secondary with a one-paragraph pointer so the Part numbering holds). Why MOVE not KEEP: the section's subject is the CANONICAL's open questions Q1/Q5/Q6 and its "where to start" step 2 — sharpenings of the parent's design living in the child, which the parent's *Still open and untouched* line did not carry |
| `## Open questions` (l.274) | KEPT — all seven are this slate's own (verb naming, the sample object, the provenance stamp, off-campus labs, integrity dials, the calibration mirror) |
| `## What this slate does NOT cover` (l.301) | KEPT — it is the boundary with the canonical, stated from this side |

### Status block
- Status: PARTIAL → PARTIAL (unchanged; Part 7 was not a `Left` item — it
  constrained the parent's)
- Left: unchanged — every item still maps to a body section (sample
  object + provenance → Part 3 · bench instruments + campus lab → Parts
  1, 5 · hand-tool tier → Part 2 · certified assayer → Parts 3, 5 ·
  salting → Part 3 · integrity/chain-of-custody → Part 3 § *Sample
  integrity*)
- Size: a build → a build
- Not retired: the whole body except Part 7 is KEEP.

## Conservation table — `implements-slate.md` (149 → 149 · Status UNBUILT → UNBUILT)

Not a cluster member (a magic implement is not a measuring instrument;
neither file cites the other). **File untouched.** Tabled so the
coordinator can diff the claim.

| heading (HEAD line) | outcome |
|---|---|
| `# Implements slate — items that make a caster better` + status block (l.1) | KEPT |
| `## The one sentence` (l.18) | KEPT |
| `## Why the cut happened, because it is the whole design brief` (l.28) | KEPT (already a pointer to magic-items.md from compaction) |
| `## What it is for` (l.43) | KEPT |
| `### The world barely touches mages, and mages barely touch the world` (l.45) | KEPT |
| `### Specialisation without a guild` (l.52) | KEPT |
| `### ⭐ The grid is already the specialisation axis` (l.66) | KEPT |
| `## The seam already exists` (l.84) | KEPT |
| `## Open questions — the design pass this slate exists to schedule` (l.95) | KEPT |
| `## Non-goals` (l.134) | KEPT |
| `## Cross-references` (l.142) | KEPT |

### Status block — unchanged (Left · Size: a build).

## Conservation table — `deduction-slate.md` (224 → 228 · Status UNBUILT → UNBUILT)

Not a cluster member (a quest type that consumes `analyze`). One
pointer line added under *Ties to existing substrates* (+4 lines);
nothing cut, nothing moved.

| heading (HEAD line) | outcome |
|---|---|
| `# Deduction — the forensic-investigation quest type (sketch)` + status block + scope-discipline box (l.1–34) | KEPT |
| `## The generic quest spine (keep it this small)` (l.38) | KEPT |
| `## The forensic type: collect → assemble → reconstruct` (l.50) | KEPT |
| `### Anti-spoiler — two layers, from two reference games` (l.58) | KEPT |
| `### The §11 hole — a permanent empty node` (l.71) | KEPT |
| `## Scope — instance the authored case, go public only for the live stream` (l.80) | KEPT |
| `## The synthesis (best elements, by source)` (l.112) | KEPT |
| `## Text-native first (deduction is already text underneath)` (l.133) | KEPT — its `analyze <thing>` bullet names the derivation, not the gate |
| `## One command language, two surfaces` (l.163) | KEPT |
| `## Ties to existing substrates` (l.180) | KEPT — **pointer added** to the *Competence-gated `analyze`* bullet: the gate's shape (route-gated `analyze`, the readout ladder, honest refusals) is instrumentation-slate's; deduction consumes it |
| `## Open questions / dials` (l.195) | KEPT |
| `## Cross-references` (l.216) | KEPT |

### Status block — unchanged (Left · Size: a build).

## Canonical after the pass — `instrumentation-slate.md` (495 → 517)

- +18 lines: `## Absorbed from sampling-and-labs-slate — Part 7 — What
  this answers in the parent slate` (verbatim body + a one-line
  provenance note).
- +4 lines in the status block: `Left` gains *the constraints absorbed
  from sampling-and-labs § Part 7 (the step-1 inventory organised on the
  portable/bench axis · certification-because-salting · Grade →
  precision at the bench, portability in the field · the hand-tool rung
  as step 2's first content)*.
- Status: UNBUILT → UNBUILT. Size: a build → a build.
- Nothing cut from the canonical.

## For the coordinator

1. **identification-slate ↔ instrumentation** (the social ledger's
   note). Read `identification-slate.md` § *`analyze X with Y`*: it
   already states the split correctly — *the instrument SHAPE is the
   instrumentation slate's; this section designs THE WRITE to the
   `IDENTIFICATION` belief realm, which the shipped `analyze` does not
   do*. The canonical carries no belief-write design, so on
   identification's side that section is KEEP, not DUPLICATE, and
   nothing needs to move into instrumentation. No action taken (file
   outside this cluster).
2. **mining-slate ↔ sampling-and-labs** (cross-cluster adjacency, not
   in my assignment). Sampling's Part 4 amends mining-slate's `assay`
   into the field tier and Open question 1 (verb naming) touches
   mining's text; mining-slate's § *The fine/coarse split: why the assay
   shed is a room* (l.401) reaches the same field/bench conclusion from
   the light-band table. Different mechanism, same shape — thematic,
   not duplicated open design as far as I can see, but mining-slate does
   not link sampling-and-labs at all. A one-line pointer from mining's
   `assay` text to sampling-and-labs would be cheap; that is the mining
   cluster's call.
3. `git diff --stat` in this worktree also shows
   `docs/slates/tails/senses-slate.md` (+9/−3) — **not mine**; a sibling
   agent's edit in the shared `master` worktree. Untouched.
